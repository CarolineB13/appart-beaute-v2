<section class="hero">
  <div class="hero-stage">
    <div class="hero-artwork" aria-hidden="true"></div>

    <div class="container hero-inner">
      <div class="hero-copy">
        <p class="hero-location">
          <span>Institut de beauté · La Valentine</span>
          <span>Marseille 11e</span>
        </p>

        <h1>
          <span>Bien plus</span>
          <span>qu’un institut,</span>
          <span>un lieu pour</span>
          <span class="script-word">vous.</span>
        </h1>

        <p class="hero-services">
          Soins · Beauté du regard
          <br />
          Onglerie · Épilation
        </p>

        <a class="hero-booking" href="/reservation/">
          <span>Prendre rendez-vous</span>
          <span class="booking-arrow">→</span>
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  @import url("https://fonts.googleapis.com/css2?family=Allura&display=swap");

  /* =====================================================
     MOBILE FIRST
     ===================================================== */

  .hero {
    overflow: hidden;

    padding-top: 74px;

    background: var(--color-bg-soft);
  }

  .hero-stage {
    position: relative;

    overflow: hidden;

    background: #f8eee6;
  }

  /*
   * Illustration verticale dédiée au mobile.
   */

  .hero-artwork {
    position: absolute;

    inset: 0;

    width: 100%;
    height: 100%;

    background-image: url("/images/hero-pampas-mobile.webp");

    background-repeat: no-repeat;

    background-position: center top;

    background-size: 100% auto;

    pointer-events: none;
  }

  /*
   * Le contenu reste dans le flux.
   */

  .hero-inner {
    position: relative;

    z-index: 2;

    padding-top: clamp(285px, 67vw, 440px);

    padding-bottom: 30px;
  }

  .hero-copy {
    width: 100%;
  }

  /* =====================================================
     LOCALISATION
     ===================================================== */

  .hero-location {
    display: flex;

    flex-direction: column;
    align-items: flex-start;

    gap: 0.18rem;

    margin-bottom: 18px;

    color: var(--color-terracotta-dark);

    font-size: 0.62rem;
    font-weight: 600;

    letter-spacing: 0.14em;

    line-height: 1.55;

    text-transform: uppercase;

    white-space: normal;
  }

  .hero-location span {
    display: block;
  }

  /* =====================================================
     TITRE
     ===================================================== */

  .hero h1 {
    max-width: 470px;

    margin: 0;

    color: var(--color-text);

    font-family: var(--font-title);

    font-size: clamp(2.9rem, 8.5vw, 3.85rem);

    font-weight: 500;

    line-height: 0.98;

    letter-spacing: -0.035em;
  }

  .hero h1 > span {
    display: block;
  }

  .script-word {
    width: max-content;

    margin-top: 7px;

    color: var(--color-terracotta);

    font-family: "Allura", cursive;

    font-size: 1.48em;
    font-weight: 400;

    line-height: 0.78;

    letter-spacing: 0;

    transform: rotate(-4deg);

    transform-origin: left center;
  }

  /* =====================================================
     UNIVERS
     ===================================================== */

  .hero-services {
    margin-top: 12px;

    color: var(--color-text);

    font-size: 0.68rem;
    font-weight: 600;

    letter-spacing: 0.17em;

    line-height: 1.9;

    text-align: center;

    text-transform: uppercase;
  }

  /* =====================================================
     CTA
     ===================================================== */

  .hero-booking {
    display: flex;

    width: min(82%, 310px);

    min-height: 52px;

    align-items: center;
    justify-content: space-between;

    margin-top: 19px;
    margin-inline: auto;

    padding: 13px 21px;

    border-radius: 999px;

    background: var(--color-terracotta);

    color: white;

    font-size: 0.74rem;
    font-weight: 600;

    letter-spacing: 0.045em;

    text-transform: uppercase;

    transition:
      background 0.25s ease,
      transform 0.25s ease;
  }

  .hero-booking:hover {
    background: var(--color-terracotta-dark);

    transform: translateY(-2px);
  }

  .booking-arrow {
    font-size: 1rem;

    transition: transform 0.25s ease;
  }

  .hero-booking:hover .booking-arrow {
    transform: translateX(4px);
  }

  /* =====================================================
     PETITS MOBILES
     ===================================================== */

  @media (max-width: 390px) {
    .hero-inner {
      padding-top: clamp(250px, 66vw, 290px);

      padding-bottom: 26px;
    }

    .hero-location {
      margin-bottom: 16px;

      font-size: 0.56rem;

      letter-spacing: 0.12em;

      line-height: 1.5;
    }

    .hero h1 {
      font-size: 2.7rem;
    }

    .hero-services {
      margin-top: 12px;

      font-size: 0.62rem;
    }

    .hero-booking {
      width: min(86%, 295px);

      margin-top: 18px;
    }
  }

  /* =====================================================
     TABLETTE
     ===================================================== */

  @media (min-width: 700px) and (max-width: 999px) {
    .hero {
      padding-top: 88px;
    }

    .hero-inner {
      padding-top: clamp(430px, 63vw, 510px);

      padding-bottom: 38px;
    }

    .hero-copy {
      max-width: 590px;
    }

    .hero-location {
      font-size: 0.66rem;
    }

    .hero h1 {
      font-size: 5rem;
    }

    .hero-services {
      margin-top: 18px;

      font-size: 0.74rem;
    }

    .hero-booking {
      width: 310px;

      margin-top: 20px;
      margin-inline: 0;
    }
  }

  /* =====================================================
     DESKTOP
     ===================================================== */

  @media (min-width: 1000px) {
    .hero {
      padding-top: 92px;

      background: #faf5f0;
    }

    /*
     * Version horizontale dédiée au desktop.
     */

    .hero-stage {
      position: relative;

      min-height: 720px;

      overflow: hidden;

      background: #faf5f0;
    }

    .hero-artwork {
      position: absolute;

      inset: 0;

      width: 100%;
      height: 100%;

      background-image: url("/images/hero-pampas-desktop.webp");

      background-repeat: no-repeat;

      background-position: center center;

      background-size: cover;
    }

    /*
     * Voile léger derrière le texte.
     */

    .hero-artwork::after {
      position: absolute;

      inset: 0 auto 0 0;

      width: 44%;

      background: linear-gradient(
        90deg,
        rgba(250, 245, 240, 0.28) 0%,
        rgba(250, 245, 240, 0.08) 72%,
        transparent 100%
      );

      content: "";
    }

    .hero-inner {
      display: flex;

      min-height: 720px;

      align-items: center;

      padding-top: 0;
      padding-bottom: 0;
    }

    .hero-copy {
      position: relative;

      z-index: 3;

      width: 46%;
      max-width: 630px;

      padding: 32px 35px 32px 0;
    }

    /*
     * Localisation sur une seule ligne sur desktop.
     */

    .hero-location {
      flex-direction: row;

      align-items: center;

      gap: 0;

      margin-bottom: 25px;

      font-size: 0.66rem;

      letter-spacing: 0.16em;

      line-height: 1.4;

      white-space: nowrap;
    }

    .hero-location span + span::before {
      content: " · ";
      white-space: pre;
    }

    .hero h1 {
      max-width: 590px;

      font-size: clamp(4.1rem, 4.45vw, 5.35rem);

      line-height: 0.94;
    }

    .script-word {
      margin-top: 9px;

      font-size: 1.42em;
    }

    .hero-services {
      margin-top: 22px;

      font-size: 0.7rem;

      line-height: 1.95;

      text-align: left;
    }

    .hero-booking {
      width: 285px;

      min-height: 54px;

      margin-top: 19px;
      margin-inline: 0;

      padding-inline: 23px;
    }
  }

  /* =====================================================
     GRAND DESKTOP
     ===================================================== */

  @media (min-width: 1400px) {
    .hero-stage {
      min-height: 760px;
    }

    .hero-inner {
      min-height: 760px;
    }

    .hero-copy {
      width: 44%;

      max-width: 650px;
    }

    .hero h1 {
      font-size: clamp(4.5rem, 4.6vw, 5.8rem);
    }
  }
</style>
